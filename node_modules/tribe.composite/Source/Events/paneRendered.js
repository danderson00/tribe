T.Events.paneRendered = function (pane, context) {
    var renderOperation = context.renderOperation;

    if (pane.model.paneRendered)
        pane.model.paneRendered();

    renderOperation.complete(pane);
    return renderOperation.promise;
};
