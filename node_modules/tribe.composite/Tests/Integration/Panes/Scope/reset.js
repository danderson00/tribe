T.registerModel(function (pane) {
    var self = this;

    this.count = ko.observable(0);
    this.elementId = pane.data + "TotalCount";

    pane.pubsub.subscribe('topic', function () {
        self.count(self.count() + 1);
    })
})
