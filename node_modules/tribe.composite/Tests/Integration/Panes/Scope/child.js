T.registerModel(function (pane) {
    var self = this;

    this.scopeCount = ko.observable(0);

    pane.pubsub.subscribe('topic', function () {
        self.scopeCount(self.scopeCount() + 1);
    })
})
