T.Events.syncRxFacet = function (pane, context) {
    if(pane.__generatedFacet)
        return pane.pubsub.syncFacet(pane.__generatedFacet).then(function () {
            context.renderOperation.complete(pane.__generatedFacet);
        });
}
