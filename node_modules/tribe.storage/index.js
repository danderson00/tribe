var open = require('./open');

module.exports = {
    open: function (entities, options) {
        options = options || {};
        options.type = options.type || 'sqlite3';
        return open(entities, options);
    }
}