var _ = require('underscore');

module.exports = {
    evaluate: function (path, target) {
        return _.reduce(path.split('.'), function (current, property) {
            return current && current[property];
        }, target);
    }
};