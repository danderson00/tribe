require('tribe').register.facet(function (facet) {
    var self = this;

    facet.isDistributed();
    facet.isScopedTo('facetId');

    facet.handles({
        'topic': function () {
            self.count++;
        }
    });

    this.count = 0;
});
