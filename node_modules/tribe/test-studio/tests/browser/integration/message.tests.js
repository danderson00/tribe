suite('tribe.browser.integration.message', function () {
    var gateway = require('tribe/client/gateway');

    test("message gateway function returns envelope populated with seq property set", function () {
        var seq;

        return gateway.publish({ topic: 'topic' })
            .then(function (result) {
                expect(result.seq).to.not.be.undefined;
            });
    });
})
