suite('tribe.browser.integration.arrows', function () {
    test("arrow functions in templates are transpiled", function () {
        var node = T.createNode('body', { path: '/tests/arrows' })
        expect($('li').length).to.equal(1);
        expect($('li').text()).to.equal('2');
    })
})
