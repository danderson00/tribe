suite('tribe.browser.integration.events', function () {
    test("pane model is extended", function () {
        var node = T.createNode('body', { path: '/tests/field' })
        expect(node.pane.model.uuid).to.be.a('function');
    })
})
