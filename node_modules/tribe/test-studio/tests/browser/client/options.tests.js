suite('tribe.browser.client.options', function () {
    var options = require('tribe').options;

    test('options are transferred to browser', function () {
        expect(options.appName).to.equal('test-studio');
    });
});
