require('tribe').register.flow(function (flow) {
    flow.startsAt('/tests/flow/one', 'test1')
    flow.on('topic').to('/tests/flow/two', { staticValue: 'test2' })
    flow.topic('topic2').count().when(1).then(flow.to('/tests/flow/one', 'test3'))
})
