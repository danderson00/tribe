require('tribe').register.flow(function (flow) {
    flow.startsAt('/tests/flow/two')
    flow.on('topic').startChild('child', { transition: 'none' })
    flow.on('topic2').to('/tests/flow/three')
    flow.on('flowWithFacet').startChild('tests/flowWithFacet', { facet: true })
})
