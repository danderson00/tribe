module.exports = {
// knockout-validation doesn't play friendly with browserify and needs _ on the global object
    initialize: function () {
        //_ = require('underscore')
        require('knockout-validation')
        ko.validation.init({ parseInputAttributes: true, decorateInputElement: true, insertMessages: false })
    }
}
