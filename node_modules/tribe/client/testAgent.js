module.exports = {
    run: function (options) {
        T.options.pubsub = require('tribe.pubsub');
        require('tribe/options').apply(options);
        require('tribe/client/hub').connect();
        require('tribe/test').initialise();
    }
};
