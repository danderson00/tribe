T.Events.syncActor = function (pane, context) {
    if(pane.__generatedActor)
        return pane.pubsub.syncActor(pane.__generatedActor).then(function () {
            context.renderOperation.complete(pane.__generatedActor);
        });
}
