var facets = require('tribe/facets'),
    options = require('tribe/options')

module.exports = function (context) {
    context.startPane = facets.definition('/__flows' + options.startFlow)
        ? { path: '__flow', data: { flow: options.startFlow } }
        : { path: options.startPane };
};
