var utils = require('tribe/utilities');

module.exports = function (path, recursive) {
    return function (context, build) {
        return utils.files.requireDirectory(build.appPath.resolve(path), recursive);
    };
};