module.exports = function (targetPath) {
    return function (app) {
        var express = require('express'),
            options = require('tribe/options'),
            utils = require('tribe/utilities'),
            path = require('path');

        app.use(targetPath, express.static(path.resolve(options.basePath, utils.paths.removeLeadingSlash(targetPath))));
    };
};
