var log = require('tribe.logger');

module.exports = function (options, context) {
    log.silly("Client released scope " + JSON.stringify(options.scope));
    context.pubsub.unsubscribe(options.token);
    context.ack();
}
