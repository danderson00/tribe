var users = require('tribe/storage').entity('users');

module.exports = {
    addUser: function (username, password) {
        return users.store({ username: username, password: password });
    },
    authenticate: function (username, password) {
        return users.retrieve({ p: 'username', v: username }).then(function (users) {

        });
    }
};