module.exports = {
    store: function (envelope) {
        // return messageId;
    },
    retrieve: function (options) {
        // { keyPath: 'property.on.envelope', value: 'any', start: messageId, end: messageId }

        // return [envelope, ...]
    }
};