var options = require('tribe/options'),
    stores = {
        sqlite3: require('./sqlite3'),
        //indexeddb: require('./indexeddb')
    },
    store;

module.exports = {
    initialise: function (indexes) {
        store = stores[options.eventStore.name];
        store.initialise(indexes);
    },
    store: function (envelope) {
        // return messageId;
    },
    retrieve: function (options) {
        // { keyPath: 'property.on.envelope', value: 'any', start: messageId, end: messageId }

        // return [envelope, ...]
    }
};