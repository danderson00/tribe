var pubsub = require('tribe/pubsub');

var statics = module.exports = {
    register: function (topic, func) {
        pubsub.subscribe(topic, function (envelope) {
            func(envelope, pubsub);
        });
    }
};
