var log = require('tribe/logger');

module.exports = {
    fail: function (req, res, status) {
        return function (error) {
            log.error('Error fetching path ' + req.path, error);
            if (options.debug)
                res.send(log.errorDetails(error));
            res.status(status || 500).end();
        };
    }
};