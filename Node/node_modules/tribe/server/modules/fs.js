var options = require('tribe/options'),
    path = require('path');

var api = module.exports = function (path) {
    return function (app) {
        app.use(path, express.static(path.resolve(options.basePath, path)));
    };
};
