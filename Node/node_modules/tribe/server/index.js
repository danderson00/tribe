var http = require('./http'),
    socket = require('./socket');

module.exports = {
    configure: function (options) {
        http.configure(options.modules);
    },
    start: function () {
        http.start();
        socket.start();
    }
};