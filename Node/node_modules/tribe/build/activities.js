var resources = require('tribe/utilities/resources'),
    path = require('path'),
    factories = {};

module.exports = {
    register: function (name, factory) {
        factories[name] = factory;
    },
    createTask: function (spec) {
        return factories[spec.activity](spec.options);
    },
    loadBuiltin: function () {
        return resources.requireDirectory(path.resolve(__dirname, 'activities'));
    }
};