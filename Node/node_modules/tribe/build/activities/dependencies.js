var build = require('tribe/build');

build.activities.register('dependencies', function (options) {
    return {
        prepare: build.blocks('addPathsToContext')('dependencies', options.path)
    };
});