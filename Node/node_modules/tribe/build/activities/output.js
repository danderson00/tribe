var build = require('tribe/build'),
    memory = require('tribe/server/modules/memory');

build.activities.register('output', function () {
    return {
        output: function (context) {
            memory.register('/build', context.output);
        }
    };
});