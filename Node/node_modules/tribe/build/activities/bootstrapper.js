var build = require('tribe/build');

build.activities.register('bootstrapper', function (options) {
    return {
        render: build.blocks('renderTemplate')('bootstrapper').to('app.htm')
    };
});