var utils = require('tribe/utilities/resources');

module.exports = function (path, recursive) {
    return function (context) {
        return utils.requireDirectory(utils.resolveAppPath(path), recursive);
    };
};