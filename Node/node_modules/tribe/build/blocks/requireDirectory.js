var utils = require('tribe/utilities');

module.exports = function (path, recursive) {
    return function (context) {
        return utils.files.requireDirectory(utils.paths.resolveAppPath(path), recursive);
    };
};