var build = require('tribe/build');

build.steps.register('resources', function (options) {
    return {
        prepare: build.blocks('addPathsToContext')('js', options.path),
        server: build.blocks('requireDirectory')(options.path)
    };
});