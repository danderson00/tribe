var build = require('tribe/build');

build.steps.register('bootstrapper', function (options) {
    return {
        render: build.blocks('renderTemplate')('bootstrapper').to('bootstrapper.htm')
    };
});